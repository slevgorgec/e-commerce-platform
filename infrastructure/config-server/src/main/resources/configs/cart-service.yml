server:
  port: 8083

spring:
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 2000ms

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

cart:
  ttl-days: 30

product-service:
  url: http://localhost:8082